spring:
  application:
    name: comsumer
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:8848
    config.:
      name: comsumer

feign:
  hystrix:
    enabled: true
